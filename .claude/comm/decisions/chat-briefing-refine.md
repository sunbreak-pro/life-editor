# Decision Queue — chat-briefing-refine

形式は [`README.md`](./README.md) 参照。回答は `ANSWERS.md` へ。

### D-20260922-briefing-1: 紙面の行削除は「消してからトースト」と「消す前に確認ダイアログ」のどちらに揃えるか

- 背景: #1820〜#1826 の #1825（PR #1932）。Materials 側から同じデータを消すと確認ダイアログが出る（#1345 と同型）のに、紙面は 1 タップで消えて画面に何も出ない。Undo は元から積まれている
- A（PR #1932 の実装・推奨）: 削除は 1 タップのまま、直後に「「{title}」を削除しました。ヘッダーの「元に戻す」で戻せます。」のトースト。紙面は「ページの行」であって「フォームの 1 レコード」ではないので、押す前に止めるより後から戻せる方が紙面の速さに合う。実装済みで、既存の Undo を画面に出すだけなので操作そのものは変わらない
- B: Materials と同じ確認ダイアログに揃える。作法が画面ごとに割れないが、朝の紙面で毎回ひと手間増える
- 放置時: A のまま。PR #1932 は merge されるまで何も変えないので実害は無い。B を選ぶ場合は別 Issue で差し替える
- 期限感: いつでも（PR #1932 の merge 前に決まると手戻りが無い）

（2026-08-16 昇格分 = D-20260815-briefing-1 〜 D-20260815-briefing-7 の 7 件・2026-08-19 昇格分 = D-20260818-briefing-1 / D-20260816-briefing-1（回答 = C・実装 Issue #1102）— いずれも `.claude/decisions/` 台帳へ。2026-08-19 分の質問・転記・昇格は chat-main が代行した）
