# Decision Queue — chat-materials-refine

形式は [`README.md`](./README.md) 参照。回答は `ANSWERS.md` へ。

### D-20260922-materials-1: ノートにパスワードを掛ける / 外す入口を復活させますか、それとも機能ごと畳みますか

- 背景: #1843。`NotePasswordDialog` の `set` / `remove` モードと `useNotePassword.ts:44-65` の分岐は実装済みですが、呼び出し元がゼロです。解錠（verify）だけが `LockedBodyGate` から届きます。入口が無いのは意図的な退役ではありません — `94e32ba4`（DU-F #25）のコミット本文が「バックエンドが throw するので一時的に外す。DU-G が 1 つの diff で再配線できるよう state と import は残す」と明記していて、その DU-G が再配線しないまま今に至ります。#526 と #1763 はどちらも入口がある前提で本文の遮断を強めた側です。
- A: 「…」メニューに掛ける / 外すを戻す（推奨 — 死にコードが消え、#1763 で強化した遮断が実際に使えるようになります）。ただし #1763 以降ロック中の本文は DB から取得しないので、**掛けた瞬間に本文が画面から消え、パスワードを忘れると復旧できません**（`docs/known-issues/027` の平文債務も残ったままです）。この 2 つを受け入れるかがこの判断の中身です。
- B: `set` / `remove` を退役させる（`NotePasswordMode` を `"verify"` へ縮約し、ダイアログの該当分岐を削除）。掛ける手段は MCP 側にも無いので、既存のロック済みノートだけが解錠可能なまま残ります。
- 放置時: 現状維持。#1843 は文言の i18n 化（PR で対応済み）だけを終えて close し、この判断は次の Issue に送ります。死にコードはそのまま残ります。
- 期限感: いつでも（急ぐ理由はありません）

（2026-08-12 昇格分 = D-20260812-materials-1 / D-20260812-materials-2・2026-08-16 昇格分 = D-20260815-materials-1 / D-20260815-materials-2・2026-08-19 昇格分 = D-20260816-materials-1（回答 = A）— いずれも `.claude/decisions/` 台帳へ。2026-08-19 分の昇格は chat-main が代行した）

（2026-09-02: #1439 の 4 点は `/goal` 指示で裁定権がレーンへ委任されたため、キューに open エントリを置かず `recorded` で直接台帳へ昇格した = [`D-20260902-materials-1`](../../decisions/D-20260902-materials-1.md)）
