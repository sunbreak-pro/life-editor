# Decision Queue — chat-schedule-refine

形式は [`README.md`](./README.md) 参照。回答は `ANSWERS.md` へ。

### D-20260902-sched-1: 「本日分の Todo」から「その他」へ移した Todo の日付をどうするか（#1406）

- 背景: #1406 で「本日分 / その他」の 2 分類にし、ホバー操作で行き来できるようにした。Todo の時刻は `scheduledAt`（日付 + 時刻の 1 つの瞬間）の中にあるので、「日付だけ外して時刻は残す」が保存できない。「その他 → 本日分」は日付だけを今日に変えて時刻を保つ（Issue の指定どおり・実装済み）。逆方向だけが割れる（`web/src/schedule/todoChipUndoWiring.ts::todoMoveOutWrite`）
- A: 日付を外して未配置に戻す（時刻も消える。Undo で戻せる）（推奨 — 旧「Todo から追加」の逆操作と対称で、勝手な日付を発明しない。**PR #1406 の実装はこれ**）
- B: 翌日へ送る（時刻を保つ。「その他」に翌日の行として残る）
- 放置時: A のまま（現行 PR の挙動）
- 期限感: いつでも

### D-20260902-sched-2: #1373 で凍った進捗の数字 2 つの扱い（#1440 の 👀 ゲート）

- 背景: #1440。「今日の流れ」見出しの `{done}/{total}` と `RoutineSummaryCard` の進捗バーは Event の `completed` を数えていて、#1373 で UI から完了概念が消えたため常に 0 を指す。Issue は A / B / C の裁定待ちだが、レーンの /goal 指示に従い**安全側の C を実装済み**（`web/src/schedule/ScheduleSidebar.tsx` / `shared/src/components/schedule/RoutineSummaryCard.tsx`）。データ列と MCP の書き込み口は温存
- A: 数字を Todo 由来だけに寄せる（「今日の流れ」= 今日の Todo の消化率。ルーチンのバーは Todo の根拠が無いので畳んだまま）
- B: 別の指標に替える（開始時刻を過ぎた件数 / 全件 — 「予定がどこまで進んだか」を時刻で表す。両方に適用可）
- C: 表示ごと畳む（**実装済み** — 何も発明しない。A / B に進むときは C の上に足す）
- 放置時: C のまま
- 期限感: いつでも


（回答済みは `.claude/decisions/` 台帳へ昇格済み — D-20260801-sched-1（2026-08-09・chat-main 代行）/ D-20260810-sched-1〜5（2026-08-10）/ 2026-08-12 昇格分 = D-20260730-sched-1 / D-20260731-sched-2 / D-20260731-sched-3 / D-20260801-sched-2 / D-20260802-sched-1 / D-20260811-sched-1 / D-20260811-sched-2 / D-20260812-sched-1 / 2026-08-13 昇格分 = D-20260812-sched-2 / 2026-08-16 昇格分 = D-20260816-sched-1 / D-20260816-sched-2 / D-20260816-sched-3 / **2026-08-19 昇格分 = D-20260818-sched-1〜3（回答 = すべて A・chat-main が代行して質問 / 転記 / 昇格）**）
