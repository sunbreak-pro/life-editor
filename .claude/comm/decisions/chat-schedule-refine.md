## D-20260730-sched-1: 編集パネルで日付を変えたとき、カレンダーを移動先の日へ飛ばすか

**背景**: #469 で編集パネルに日付ピッカーを付けた。日付を変えると、その行は今表示している週から外れて見えなくなる。

**A（採用・現行実装）: 飛ばさない**。カレンダーは今の週のまま。エディタは開いたままで、日付欄が新しい日を表示する。行がグリッドから消えるのは次の同期後。

- 利点: 壊れない。飛ばす実装（#484 初版）は、可視範囲が変わると再フェッチが `rangeItems` を置き換えて**保存が着地する前の楽観更新を捨てる**ため、行がどこにも無い瞬間に詳細パネルが自分で閉じた（QA の Blocking B-2）
- 欠点: 移動先を目で確認できない（日付欄の数字で確認するしかない）

**B: 飛ばす（保存の着地を待ってから）**。`updateScheduleItem` を await 可能にして、DB に着いてから anchor を動かす。

- 利点: 移動先が見える
- 欠点: `useScheduleItemsAPI` の書き込み API を await 可能にする改修が必要（現状 void の fire-and-forget）。失敗時の toast 設計も必要

**放置時**: A のまま（安全側）。移動先を見たい要望が出たら B を Issue 化して実装する。

## D-20260731-sched-2: #468 の台帳フィルタに「予定へタグを付ける導線」を同梱するか

**背景**: #468 でカレンダー台帳をグリッドのフィルタとして配線する。ところが実測したところ、**アプリ内に予定（event）へ life-tag を付ける導線が存在しない**（`assignTagToItem` の呼び出し元は Notes 詳細とタスク Kanban の TagPicker だけ・Schedule のコンテキストメニューの「タグを追加」は stub）。このままフィルタだけ入れると、カレンダーを選んだ瞬間に予定が全部消えてタスクチップだけ残る。

**A（採用・既定）: 同梱する**。編集パネル（`EventEditorPane`）に `tagSlot` prop を 1 本足し、既存 `TagPicker` を注入するだけの最小追加。繰り返し由来の行は `routineId` を対象にして系列単位で付ける。

- 利点: #468 の DoD 1 番目「台帳の選択でグリッドが絞り込める」を予定について実証できる。差分は slot prop 1 本 + 注入 1 行
- 欠点: #468 の差分が編集パネルにも伸びる（#467 の Mobile 領域とは別ブランチなので衝突はしない見込み）

**B: フィルタだけ先に出す**。予定のタグ導線は別 Issue に切る。

- 利点: 差分が shared/utils + Desktop ツールバー + 台帳モーダルに閉じる
- 欠点: 台帳フィルタが実質タスク専用のまま出る。「配線したのに効かない」が 1 PR 続く

**放置時**: A で実装して PR を出す（フィルタが機能する状態で着地させるのが安全側。B に倒す場合はタスク 7 を落とすだけで済む）。

## D-20260731-sched-3: 台帳フィルタをタスクチップにも効かせるか

**背景**: #468 の初版はカレンダーを選んでも青いタスクチップが残る（合流点が絞り込みの後段のため）。QA の指摘で判明。タスクも `TagPicker` で同じ life-tag を持てる（`web/src/tasks/KanbanView.tsx:436`）ので、「タグは持てないから対象外」という説明は成立しない。

**A（採用・既定）: タスクチップにも効かせる**。`hiddenByCalendar` に合算する。

- 利点: 「Work を選んだのに Home のタスクが並んでいる」が消える。チップの数字と画面上のブロック数が一致する
- 欠点: Schedule の Todo トレイと見え方がずれる瞬間がある（トレイ側は非適用のまま）

**B: 予定だけに効かせ、その旨を明記する**。

- 利点: Issue の文面（「どの予定をいま見るか」）に忠実
- 欠点: 画面上は絞り込みが半分しか効いていないように見える

**放置時**: A で実装（画面の整合を優先）。B に倒す場合はタスクチップへの適用を外してチップ行に一言添えるだけで戻せる。

## D-20260801-sched-2: Connect グラフのダブルクリックを「開く」に返すか、d3 の拡大のまま残すか

**背景**: #524 の実装中に実測。ノードのダブルクリックは `onActivate`（アイテムを開く）を一度も呼んでいない。`d3-zoom` が canvas に登録する `dblclick.zoom` が `stopImmediatePropagation()` を撃ち、後から登録している自前の dblclick リスナーへ届かないため。今そこで起きているのは **d3 既定のダブルクリック拡大**。#523 / #524 の退行ではなく最初からこの形で、「開く」導線自体は選択カードのボタンが生きている。

**A（推奨）: ダブルクリックを「開く」に返す**。`sel.on("dblclick.zoom", null)` の 1 行で d3 の拡大を切る。

- 利点: `onActivate` の契約（「double-click / "open" intent on a node」）が実際の挙動と一致する。拡大はホイールとビューリセットで足りる
- 欠点: ダブルクリック拡大に慣れていると、拡大手段が 1 つ減ったように感じる

**B: 現状維持（拡大のまま）**。`onActivate` の口をダブルクリックからは外し、コメントとドキュメントを実態に合わせる。

- 利点: 触らない。d3 既定の操作感を保つ
- 欠点: 「効かないショートカット」が残る（押しても開かない = #434 S-1 と同じ壊れ方）

**放置時**: 何もしない（B 相当・現状維持）。実装は Issue 化してから着手する。

## D-20260802-sched-1: outbox エントリを実装 PR に同梱するか、tracker と同様に merge 後へ回すか

**背景**: D-20260801-main-1 は tracker（memory/history）を実装ブランチに載せないとだけ規定していて、`.claude/comm/outbox/` の扱いは未規定。outbox も「同じファイルの先頭への追記」なので、複数ブランチが並行すると tracker と同じ衝突パターンになる。今セッションは #555 / #551 / #553 の 3 本を同一 worktree から連続して出すため、各 PR に outbox エントリを載せると 2 本目以降が必ず衝突する。

**A: 実装 PR に同梱する（従来型）**。エントリがその PR と一緒に main へ届く。

- 利点: chat-main が merge 時点で outbox の起票依頼まで読める
- 欠点: 並行 PR で outbox が衝突する（tracker で実証済みのパターン）

**B（既定・今セッションはこれで進行）: tracker と同じく実装 PR では触らず、merge 後にまとめて 1 commit**。それまで worktree の作業ツリーに未コミットで保持する。

- 利点: 衝突ゼロ。D-20260801-main-1 と一貫
- 欠点: chat-main への伝達が merge 後まで遅れる（緊急の連絡は PR 本文 / PR コメントに併記して補う）

**放置時**: B（安全側）。今セッションの outbox エントリは未コミットのまま保持し、要点は PR 側に併記する。

## D-20260811-sched-1: #625 Event → Todo で日付・時間帯を Todo のチップスロットへ引き継ぐか（D-20260810-sched-3 の緩和提案）

**背景**: D-20260810-sched-3（= A: 時刻・終日・繰り返しは破棄）の前提は「Todo には時刻に対応する行が無い」だったが、実装中の実測で **`tasks_payload` は `scheduled_at` / `scheduled_end_at` / `is_all_day` を持っている**（task チップ = カレンダーに Todo を配置する機能の行）と分かった。前提が変わったので緩和案を提示する。現実装（PR #684）は裁定どおり破棄で、逆方向（Todo → Event）はスロットを引き継ぐため往復非対称。

**A: 現状維持（破棄）**。ダイアログは「日付・時刻・繰り返し設定は失われます」。

- 利点: 裁定どおり・実装済み。「変換したらカレンダーから消える」が分かりやすい
- 欠点: 往復非対称（Todo→Event は場所を保つのに Event→Todo は消える）

**B: 日付・時間帯を task チップスロットへ引き継ぐ**。8/20 10:00 の予定は 8/20 10:00 に配置済みの Todo チップになる。繰り返しは引き続き破棄。

- 利点: カレンダー上の見た目がほぼ変わらず「性質だけ変わる」。失うものが繰り返しだけになりダイアログも短くなる
- 欠点: D-20260810-sched-3 の supersede が要る。「Todo にしたのにカレンダーに残っている」を意外に感じる可能性

**放置時**: A のまま（実装済みの現状維持・安全側）。

**期限感**: いつでも。

## D-20260811-sched-2: #692 Mobile 月ビューの入口をどの形にするか（#467 が消した切替の部分復活）

**背景**: #692 Step 1。Mobile には月の俯瞰が無い（narrow は `effView` が `"list"` 固定 = `web/src/schedule/useCalendarNav.ts:32`）。#467（Epic #290 Step 5-c）が切替 UI ごと退役させたので、月ビューを足すには**入口を何かしらの形で戻す**ことになる。どの形で戻すかがユーザー判断。

**実測で分かった前提（2026-08-11・本ブランチ）**:

- `MonthGrid` には既に **`compact` prop（"Mobile density: day badge + dot row instead of chips"）が実装済み**で、テストもある（`shared/src/components/schedule/MonthGrid.tsx:62-63` / `shared/tests/monthGrid.test.tsx:73-110`）。**ただし `CalendarTab.tsx` はどこからも渡していない** — 作られたまま配線されていない Mobile モード。セルは `min-h-14`（56px・`MonthGrid.tsx:172`）でタップ標的として足りる。**よって #692 は MonthGrid の作り替えではなく配線で足りる**
- **`effView` を narrow で `"month"` にできれば、移動単位と取得範囲は自動で追従する**（下記トレース）。「3 箇所を揃って直す」必要は無い

**`effView` の 4 消費点トレース（Issue 本文の懸念の答え）**:

| #   | 場所                                                                                           | 現状                                                                                                    | narrow で `"month"` にした時                                               |
| --- | ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| 1   | `useCalendarNav.ts:32` ピン留め                                                                | `isWide ? desktopView : "list"`                                                                         | **ここだけが唯一直す必要のある行**                                         |
| 2   | `useCalendarNav.ts:61-68` `step()`                                                             | `effView === "month"` を **`isWide` で囲っていない**（:64）                                             | 自動で月送りになる（追加改修 **不要**）                                    |
| 3   | `useCalendarNav.ts:48-59` → `visibleCalendarRange`（`shared/src/utils/calendarView.ts:44-47`） | `effView === "month"` を **`isWide` で囲っていない**（`:44`。`isWide` を見るのは week 分岐 `:49` だけ） | 自動で月グリッド全体（`monthRows[0][0]`〜末尾）を取得（追加改修 **不要**） |
| 4   | `CalendarTab.tsx:918-922` `periodLabel`                                                        | 同じく `isWide` 非依存                                                                                  | 自動で「2026年8月」表記になる（追加改修 **不要**）                         |

補足: `monthRows` は `effView` と無関係に常時計算されている（`useCalendarNav.ts:42-45`）ので、narrow でも即使える。**残る配線は「narrow の描画分岐に MonthGrid を出す」1 点**（現状の narrow 分岐は `AgendaList` 決め打ち = `CalendarTab.tsx:2500`。wide 側の month 分岐は `desktopView` を読んでいて `effView` ではない = `:2375`）。

**A（推奨）: ヘッダの日付ラベルのタップで月シートを開く**。narrow ヘッダの `periodLabel`（`CalendarTab.tsx:2464-2466`。現在ただの `<span>`）をボタン化し、`BottomSheet` に `MonthGrid compact` を載せる。セルタップでその日へ移動してシートを閉じ、#691 の Dayflow に戻る。

- #467 が消した切替はどうなるか: **復活させない**。「単画面 + FAB」の原則（Epic #290 Step 5-c）は保ったまま、俯瞰だけをシートで足す。ビューという概念は Mobile に戻らない
- コスト: シートの開閉 state 1 個 + `MonthGrid` 注入。`useCalendarNav.ts:32` を触らずに済む**可能性がある**（シートに `monthKey` を渡すだけなら `effView` は `"list"` のまま。ただしその場合 **取得範囲は 1 日のままなのでシート内のセルが空になる** → シート表示中だけ月レンジを取る仕組みが要る = :32 と同等の分岐が結局要る）
- 壊れるもの: `periodLabel` がボタンになるので、narrow ヘッダの高さ・タップ標的が変わる。既存の narrow ヘッダのスナップショット的なテストがあれば要更新

**B: narrow に表示切替コントロールを戻す（#467 の部分的な巻き戻し）**。日 / 月の 2 択に絞ったトグルを narrow ヘッダに置き、`useCalendarNav.ts:32` を `isWide ? desktopView : mobileView` に変える（`mobileView` は narrow 専用 state で `"list" | "month"`）。

- #467 が消した切替はどうなるか: **2 択に縮めて戻る**。#467 が退役させたのは day / week / month / timeline の 4 択切替で、そのうち「narrow で指では扱えない」と判断された week / timeline は戻さない
- コスト: narrow 専用 state 1 個 + トグル 1 個。`view`（Desktop の選択）は別 state のままにするので、wide に戻したとき Desktop の選択が壊れない
- 壊れるもの: 「Mobile は単画面」という #467 の説明が成り立たなくなり、`useCalendarNav.ts:26-31` の長いコメントと `CalendarTab.tsx:2436-2447` の narrow の意図説明が実態と食い違う（両方書き換えが要る）。`mobile-scope.md` #4 の記述も要更新

**放置時**: **現状維持（Mobile に月ビューを足さない）**。#692 は保留のまま Step 2 に入らず、次の作業単位へ進む。

**期限感**: いつでも（#691 → #692 → #675 の順を守るため、#691 の回答より後で構わない）。

---

（回答済みは `.claude/decisions/` 台帳へ昇格済み — D-20260801-sched-1（2026-08-09・chat-main 代行）/ D-20260810-sched-1〜5（2026-08-10・チャット回答を受けて当チャットが昇格））
