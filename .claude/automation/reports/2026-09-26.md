# Routine report 2026-09-26

## night-safe / 2026-09-26 17:43

<!-- log: night-safe-2026-09-26_1740.log / exit code: 0 -->

## 2026-09-26 17:43 Night Safe Run

- Elapsed: 3 min / 45 min（スキップしたタスクはありません）
- docs 整合: 5 件見つかりました（新規 2 件、前回からの持ち越し 3 件）
- Issue 台帳: 5 件見つかりました（新規 3 件、前回からの持ち越し 2 件）
- PR conflict: 0 件です。open の PR が 0 本なので、衝突も rebase が要る PR もありません。
- 検証準備: 変化が 4 件あります。
- この回は 17:40 に始まりました。いつもの 22:33 の枠ではありません。

### docs 整合（5 件）

1. **Desktop 配布の計画書 `plans/2026-08-30-desktop-app-packaging.md:2`（新規）**
   - 矛盾: Status 行と Worklog は `desktop-v0.1.0` の draft Release で止まっています。実際には 2026-09-13 に tag `desktop-v0.2.0` が打たれました。さらに 2026-09-26 に PR #1993 が version を 0.2.1 へ上げていて、tag `desktop-v0.2.1` の push を待っています。
   - 修正案: Status 行を「0.2.0 は発行済み。0.2.1 は tag push と実機受け入れ待ち（こうだいさんの作業）」に書き換え、Worklog に 09-13 と 09-26 の 2 行を足します。
2. **同じ計画書の `:183`（持ち越し）**
   - 矛盾: 「Release に arm64 `.dmg` と `.exe` が載る」が未チェックのままです。2026-09-09 の時点で両方が載ったことは実測済みです。
   - 修正案: この行を `- [x]` にします。
3. **Schedule リファクタリング第 1 期の計画書 `plans/2026-09-16-schedule-refactor.md:2`（新規）**
   - 矛盾: Status が理由のない `IN PROGRESS` のままです。第 2 期の計画書 `2026-09-23-schedule-refactor-phase2.md:6` には「W0〜W14 と W16 は着地済みで、残る W15（archive 化）は本書の P0 が引き取る」と書かれています。
   - 修正案: Status 行に「残 = W15 のみ（第 2 期 P0 が引き取る）」と書き足します。
4. **スマホから MCP を使う計画書 `plans/2026-09-09-remote-mcp-mobile.md:2`（持ち越し）**
   - 矛盾: Status は理由のない `IN PROGRESS` のままです。残りの作業（デプロイ、コネクタ登録、実機確認）は 2026-09-26 に #1994 として起票されました。
   - 修正案: Status 行を「残 = Step 4 / 5（#1994・人手の作業）」にします。
5. **モバイル対応範囲の正本 `docs/requirements/mobile-scope.md:59`（持ち越し）**
   - 矛盾: 「header = wide 専用の `HeaderUndoRedo`」と書いてあります。実際には `web/src/MainScreen.tsx:305` が狭い幅のヘッダーにも同じ部品を渡しています。
   - 修正案: 備考を「狭い幅のヘッダーにも同じ `HeaderUndoRedo` が出る」に直します。

前回指摘した夜間レーンと朝のダイジェストの手順書 2 本の「登録はまだ」という注記は、PR #1971 で直っていました。CLAUDE.md が参照するコードと設定のファイル 23 本は、09-23 のデッドコード削除（#1991）の後もすべて残っています。前回の「未追跡ファイル 7 つ」の件は chat-main 側の作業コピーにあるので、この worktree からは確かめていません。

### Issue 台帳（5 件）

1. **#1974 は close できる状態です（新規）。** 対応した PR #1984（09-23 merge）の本文が「#1813 と合わせて #1974 の DoD をすべて満たす」と書いています。閉じる判断を人に委ねたため `Refs` にしてありました。
   - 修正案: #1974 を close します。
2. **#1850 は close できる状態です（新規）。** 2026-09-21 のコメントに「#1886 の着地にあわせて close してください」とあり、#1886 は 09-23 に close 済みです（PR #1967 がタグ統合の機能を削除しました）。
   - 修正案: #1850 を close します。
3. **#1335 は close できる状態です（持ち越し・前提が変わりました）。** 09-23 のコメントに「PR #1971 の merge 後に close してください」というユーザー回答が書かれていて、#1971 は同じ日に merge 済みです。実装レーン（`night`）の登録を再判断する期限の 9/26 は今日です。
   - 修正案: night の登録を別 Issue に切り出してから #1335 を close します。
4. **Epic #1121 は close できる状態です（持ち越し・前提が変わりました）。** 子 Issue 4 本は close 済みで、09-23 に chat-main が実ブラウザで Epic の DoD を満たすと書いています。そこから切り出した #1976 / #1977 も PR #1978 で close 済みです。
   - 修正案: 対象外にしたセクション（Briefing / work / connect / analytics）を後続の Epic に切り出してから close します。
5. **宛先ラベルの無い open Issue が 3 本あります。** #1994 と #1987 が新規で、#1335 は持ち越しです。3 本とも `[main]` の prefix だけを持ち、`section:` も `shared-fix` もありません。そのため、レーンへの配布（issue-prompter）の対象になりません。#1987 は `desktop/` を触る実装の Issue です。
   - 修正案: chat-main が自分で持つなら今のままでかまいません。どこかのレーンに回すなら `shared-fix` を付けます。

#1793（環境音）は、09-23 に継ぎ目の途切れを直す PR #1965 が merge されました。本文に「音源の差し替えが済むまで閉じない」とあるので、open のままで正しいです。

### 検証準備（4 件）

1. **09-23 に merge された約 25 本が、目視待ちのリストに入っていません。** 対象は connect #1960〜#1970、materials と notes #1980〜#1983、ツアー #1978 / #1979、テーマのコントラスト #1951 / #1957 などです。`memory/chat-main.md` の「ユーザー実機目視待ち」の節（:107〜126）は、09-20 以降の波をまだ取り込んでいません。
   - 次のアクション: chat-main が merge 済みの分をリストに流し込みます。
2. **`memory/chat-main.md` の目視待ちに、前提が消えた項目が 3 つあります。**
   - `:120` の「Phase 3 Electron（`build:mac` で DMG）」は、2026-09-07 の Mac 実機受け入れで全項目が通っています。
   - `:113` の #512 は 2026-08-12 に close 済みです。
   - `:119` の W4 と `:103-105` の #524 は、グラフが退役したので無効です（持ち越し）。
   - 次のアクション: 4 項目とも取り消し線を引きます。
3. **`memory/chat-main.md:123` の W3-C（環境音のミックス再生）は、確認のしかたが変わりました。** 再生の仕組みが PR #1965 で 2 本の audio 要素のクロスフェードに替わっています。
   - 次のアクション: 1 周の継ぎ目で音が切れないことを確認項目に足し、#1793 の確認とまとめて 1 回で見ます。
4. **`memory/chat-main.md:14` の書き方が古いままです。** 「tag `desktop-v0.1.0` を打つ」「#1301 は open のまま」と書いてありますが、tag は 09-09 に push 済みで、#1301 は 09-13 に close 済みです。今の検証待ちは PR #1993 の後の `desktop-v0.2.1` です。Windows で閉じてから起動し直したとき、設定とツアーの進み具合が残るかを確認します（#1636 / #1775 の実機確認）。
   - 次のアクション: :14 を 0.2.1 の手順に置き換えます。

### 起票依頼（chat-main が翌朝判断するもの）

- 計画書 3 本の Status を実態に合わせる件（Desktop 配布、Schedule 第 1 期、スマホ MCP）。上の docs 整合 1〜4 です。
- `mobile-scope.md` の #16 行をコードに合わせる件（docs 整合 5・持ち越し）。
- #1974 / #1850 / #1335 / #1121 を close するかの判断。#1335 と #1121 は、先に後続分を別 Issue に切り出します。
- `[main]` の Issue 3 本に宛先ラベルを付けるかの判断。
- こうだいさんの作業: `desktop-v0.2.1` の tag を push し、Windows 実機で受け入れを確認します。

open の PR が 0 本なので、merge 待ちの作業はありません。今回はファイルを 1 つも書いていません。

## digest / 2026-09-26 17:53

<!-- log: digest-2026-09-26_1751.log / exit code: 0 -->

# Dev Digest — 2026-09-26

<!-- run: digest / 2026-09-26 -->

## 3 行サマリ

- 今日いちばん効く 1 手は、**本番 DB に migration 0030 / 0031 が入っているかを確かめて、入っていなければ `cd supabase && npm run db:push` を打つこと**です。0031 を使うノート本文検索（PR #1983）は 09-23 に merge 済みです。一方、PR 本文が「merge の前に db push」と求めていたのに、push したという記録がどこにもありません。
- open PR は 1 本だけです（#1995・今日の夜間点検レポート）。判断キューも未回答 0 件なので、merge の渋滞はありません。
- 残っている作業の中心は、配布前の Issue 4 本（#1986 SMTP → #1987 Electron → #1988 エクスポート → #1989 ヘルプ）と、こうだいさんの手作業 2 つです。手作業は `desktop-v0.2.1` のタグの push と、スマホから MCP を使うための Worker のデプロイ（#1994）です。

## 要判断（5 件）

**1. 本番 DB に 0030（タグの表示色）と 0031（ノート本文検索の SQL 関数）を適用するか**
09-23 の棚卸しでは、0030 は本番に入っていないと実測されました。0031 は同じ日にコードだけ merge されています。
A: 今日 `db:push` を打つ（推奨 — 未適用の分だけが流れるので、適用済みでも害がないためです）／ B: 次の DDL と一緒にまとめる。
放置時: 本文だけに含まれる語でノートを検索しても 0 件になり、一覧に警告が出たままです。

**2. `desktop-v0.2.1` のタグを push して、Windows 実機で受け入れ確認をするか**
PR #1993 で version は 0.2.1 に上がっています。二重起動で設定が消える件（#1775 / #1636）の修正は、インストールしたビルドでしか確かめられません。
A: 今日 push して、閉じて開き直したあとも設定とツアーの進み具合が残るかを見る（推奨 — 修正が入ってから 1 週間、実機で確かめていないためです）／ B: 配布前 4 本が片付くまで待つ。
放置時: draft Release は 0.2.0 のままです。

**3. close できる状態の Issue 4 本をどう閉じるか**
#1974 は PR #1984 で完了条件を満たしています。#1850 は、前提だったタグ統合の機能が PR #1967 で削除されました。#1335 と #1121 は、後続分を別の Issue に切り出してから閉じる形です。
A: #1974 / #1850 だけを今日閉じ、#1335 / #1121 は chat-main が切り出してから閉じる（推奨 — 後の 2 本には、まだ行き先のない残作業が含まれているためです）／ B: 4 本とも今日閉じる。
放置時: open のまま残ります。配布の作業には影響しません。

**4. 夜間の実装レーン（night）を Task Scheduler に登録するか（再判断の期限は今日 09-26）**
登録の条件は「夜間点検の報告が毎日そろうこと」でした。実際には 09-20〜09-22 の 3 回のあと、09-23〜09-25 の報告が見当たりません。今日の回も 22:33 ではなく 17:40 に走っています。
A: 見送って、原因を確かめてから次の 1 週間を観察する（推奨 — 条件が満たされていないためです）／ B: 今日登録する。
放置時: 見送りと同じ扱いになります。

**5. スマホから MCP を使うための Worker のデプロイ（#1994）をいつやるか**
コードは 09-12 から main に入っています。残りはシークレットの投入、デプロイ、コネクタの登録、実機確認で、どれも手作業です。`/health` はまだ 1042 を返しています。
A: 今週中にやる（推奨 — 残りが手作業だけで、配布前 4 本とは別の場所を触るためです）／ B: 配布前 4 本のあとに回す。
放置時: スマホの Claude アプリからは使えないままです。

## merge 判断表

| 順 | PR | 衝突 | リスク |
| --- | --- | --- | --- |
| 1 | #1995 夜間点検レポート 2026-09-26 | なし（MERGEABLE） | `.claude/automation/reports/` に 1 ファイル足すだけで、コードには触れません |

## レーン別「次の一手」

- **web-public**: 配布前 1/4 の独自 SMTP を担当します（#1986・shared-fix）。
  `/goal #1986 を 1 ブランチ = 1 PR で着地させて。SMTP の資格情報の投入はこうだいさんの手番として PR 本文に手順を書くこと`
- **settings-refine**: エクスポート（#1988・important）のあとにヘルプ導線（#1989）を進めます。
  `/goal gh issue list -R sunbreak-pro/life-editor --label section:settings --state open で自分宛を拾い、#1988 → #1989 の順に 1 Issue = 1 ブランチ = 1 PR で着地させて`
- **schedule-refine**: 第 2 期の計画書 `2026-09-23-schedule-refactor-phase2.md` の P0 から始めます。
  `/goal 2026-09-23-schedule-refactor-phase2.md の P0 から順に、1 工程 = 1 PR で着地させて（#1642）`
- **chat-main**: 次の 4 つを片付けます。
  - #1987 の Electron を 44 系へ上げる作業（宛先ラベルなし）
  - 回答済みの判断 2 件（D-20260920-main-2 / D-20260905-shared-fix-1）を台帳に上げて、キューから消す作業
  - 計画書 3 本の Status と `mobile-scope.md` の #16 行を実態に合わせる作業（夜間点検の指摘）
  - 目視待ちリストの更新

  `/goal 夜間点検 2026-09-26 の起票依頼を裁き、判断キューの残り 2 件を台帳へ昇格してから #1987 に着手して`
- **work-refine**: #1793 は音源を差し替えるまで待ちです。

## 実機目視の残

09-20 以降に merge された約 110 本が、目視待ちリストに入っていません。内訳は 09-22 の波が約 84 本、09-23 の波が約 25 本です。リストの中には、前提がなくなった項目も 4 つあります（W4 / #524 / #512 / Phase 3 Electron）。

朝刊へのミラーはしていません。この Windows 機では life-editor MCP を無効にしているためです。本番 DB の migration 適用状況も、Supabase MCP が無人実行の権限で止まったため、自分では確かめていません。
