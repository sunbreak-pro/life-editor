# MEMORY (chat-schedule-refine)

## 進行中

（なし — **PR #450（#434）の merge 待ち**。次の #408 は `CalendarTab.tsx` と i18n catalog を #450 と共有するため、着地前に origin/main から切ると衝突する）

## 直近の完了

- **#434 #407 QA follow-up 2 件（変換中の pending 表示 + 失敗 toast / ガードの shared 切り出し）** ✅（2026-07-28 — **PR #450** 作成済み・merge は 🛑 ユーザーゲート）。**見送り可だった 2 も実装した**: 1 で state ミラーが増え、ref と state がずれると「ずっと変換中」か「一度もロックされない」に化けるので、テストで固定する価値が出たため（#355 `useDeferredAction` と同じ「web にテストランナーが無いから shared の純フックへ寄せる」方針）。新設 `shared/src/hooks/useInFlightGuard.ts` の肝は **`begin(id)` が check と claim を 1 呼び出しに畳む**点で、「has で見てから add」の 2 文に戻せない＝ #407 を生んだ check-then-act の隙間を呼び手が再導入できない。**同期の ref が権威・state は描画専用のミラー**（2 クリックが同じ tick に届く以上、バッチされる state では 2 個目を素通しする）。UI 側は `FrequencyEditor` の `pending` で全コントロールを disabled + `aria-busy` + `role="status"` の理由表示、`SegmentedControl` に `disabled` を追加。失敗 toast は `onRepeatConvertFailed` 注入 →`CalendarTab` が `showToast` に配線（#376 `noteAttachFailed` と同契約）。**role-qa アドバーサリアルラウンド反映済み**（2 コミット目 `69ca32e6`・B=0 / S=2 / N=4 で S 2 件とも修正）: **S-1 は自分が入れた退行** = 本物の `disabled` で節をロックすると focusable がゼロになり、キーボード操作中にフォーカスが `<body>` へ飛んで戻らない → セグメント / 曜日チップは `aria-disabled` + ハンドラ無効化（tablist の roving tabindex を保つ）、入力は `readOnly` に変更（**先例 `Menu.tsx:185` は `disabled` と `aria-disabled` の併用だが、tablist は全segment が同時にロックされるので併用だと退避が起きる — 先例をそのまま真似ない判断**）。S-2 = `ensureRoutineItemsForDateRange` の reject が void promise の外に抜けて unhandled rejection ＋ `reload()` スキップ → catch して `reload()` を `finally` へ移動（どの出口でもちょうど 1 回）。**この経路は繰り返し自体は成立している**ので toast 文言を理由で出し分ける `onRepeatConvertFailed(reason: "attach" | "materialise")` に変更。検証 = shared vitest **151 files / 1234 pass（main 比 +25）**・shared build・web build・web lint すべて exit 0。**スコープ外にした観察**（PR 本文に記載・未起票）= 系列パス（既に routine が付いたアイテムの頻度変更）の `updateRoutine` false は今も静かに降りる（`useScheduleMutations.ts:516`）
- **#433 #407 のアドバーサリアル QA 修正が main に届いていない（置き去りコミット回収）** ✅（2026-07-28 — **PR #435 merge 済み・Issue close 済み**。merge commit `8075adea`・`git merge-base --is-ancestor` で main 到達を実測、`routineFrequency.ts:115` の `if (!start)` と `frequencyEditor.test.tsx:107` の空文字テストが origin/main 側に存在することも実測。旧ブランチとの対象 7 ファイル差分はゼロ）。**残 1 件** = `claude/schedule-407-repeat-desync` の削除（tip `52b6d081`・着地確認済みなので安全。Claude 側は権限で実行不可のためユーザー作業）。PR #423 の merge（7/27 18:20 JST）**直後**に push した 2 コミット（`a873e583` 18:35 / `52b6d081` 18:37）が main に届いていなかったのを、`origin/main`（`415cb185`）から切った `claude/schedule-433-recover` へ cherry-pick で回収。**両方ともコンフリクトなしで乗った**。着地前の main は「#423 の fail-closed 判定 + `FrequencyEditor` の空文字 emit」が噛み合い、**開始日をクリアすると reconcile が未来行を掃除する**退行が起きうる状態だった。検証 = shared vitest 149 files / 1209 pass・shared build・web build・**web lint も exit 0**（下の引き継ぎメモにある `NotesView.tsx:291` の main 由来 lint error は 2026-07-28 時点の origin/main `415cb185` では**再現しない**。どの PR で消えたかは未追跡）
- （#407 繰り返し表示の不整合は PR #423 + #435 で main 着地済み・history の 2026-07-27 / 2026-07-28 に全文あり。**DB クリーンアップだけ残る** = Routines タブから「新規予定」routine 2 本 `3c4a1f09` / `b15eb258` をユーザーが削除する。コード修正で新規生成は止まっているが、routine 本体と生成済み si- 行は UI からの削除が要る）
- （#367 見送り決着（PR #403 merge 済み）/ #376 統合アイテム生成パネル（PR #393 / #395 / #400 merge 済み）は history の 2026-07-27・2026-07-26 に全文あり。3 件枠に収めるため間引いた）
- #298 Step 3 rightSidebar 本日の Todo tray ✅（2026-07-23 — PR #323 merge 済み・main `5f9abf48`）。**history 側にエントリが無いのはここだけ**なので、この行を消すと merge 済みの記録がこの worktree から消える（#296 / #297 は history の 2026-07-20 に残る）
  （#352 / #353 / #354 / #355 / #385 / #299 は history の 2026-07-25〜26 に全文あり。ここからは間引いた）

## 予定

- **今回の担当は #433 → #434 → #408 → #411 の 4 件**（2026-07-28・chat-main の fan-out 計画書 `docs/vision/plans/2026-07-28-open-issue-fanout.md` の schedule-refine 節。前回「粒度あるタスクが無い」と書いた状態は #290 Step 5 が子 Issue に割られて解消）。**#433（PR #435 merge 済み）/ #434（PR #450 open）は完了。残りは #408 → #411**
- **#408 は PR #450 の着地待ちで着手できない**: #450 が `CalendarTab.tsx`（`repeatLabels` / mutation フック呼び出し / EventEditorPane への props）と i18n catalog を触っており、#408 も同じ 2 ファイルを触る（`openRoutinesCta` 等の退役・`RoutineSummaryCard` の CTA 撤去）。着地前に origin/main から切ると 2 本の PR が同じ行域で衝突する
- **#408 の事前調査は済み**（2026-07-28・読み取りのみ）。**Routines タブ廃止で失われる操作は 5 件**で、rightSidebar の繰り返し一覧パネルはこれを埋める設計にする必要がある: (1) オカレンスを持たない routine の新規作成（`RoutinesTab.tsx:110` / ボタン `:174` — Calendar 側の起点は必ず既存 schedule_item なので空 routine を作る手段が無い）(2) **全 routine への到達性**（Calendar は「可視範囲に materialise 済みのオカレンス」経由でしか触れず、開始日が未来の interval / `isVisible=false` / `isArchived=true` の routine に到達できない。`RoutineSummaryCard` は読み取り専用かつ `!isArchived && isVisible` フィルタ付き）(3) テンプレの title / start / end を **scope ダイアログ（this / future / all）を挟まずに**直接編集（`RoutineEditorForm.tsx:111` / `:124` / `:135`）(4) テンプレ単位の直接削除（`RoutinesTab.tsx:167`）(5) `order` 昇順での俯瞰（`RoutinesTab.tsx:83`・Calendar 側は配列順で無ソート）。**挙動差 1 点** = 頻度編集後の reconcile 窓が RoutinesTab は「今日から 41 日固定」（`RoutinesTab.tsx:49`）、CalendarTab は「可視範囲」（`useScheduleMutations.ts:497`）。**道連れ退役候補**（参照ゼロを grep 実測）= `RoutineEditorForm` + 型 3 種 / `createRoutine` の UI 呼び出し / i18n `scheduleScreen.newRoutine`・`deleteRoutine`・`selectHint`・`tabDetail`、タブ撤去で死ぬ `scheduleScreen.routines`・`openRoutinesCta`・`onOpenRoutines` 経路・`ScheduleTab` の `"routines"` 分岐
- **#411 の肝は `setMaterialsTab` の全数追随**（取りこぼすとタスク導線が全部死ぬ）。実測した呼び出し箇所は `web/src/MainScreen.tsx` の `:246`（nav ショートカット）/ `:269` / `:289`（`MATERIALS_TAB_FOR_ROLE` 経由 = `[[link]]` の着地）/ `:318`、タブ定義は `:139` `:188` `:354` `:423` `:468-470` `:537-539` `:581` `:593` `:611`
- **実ブラウザ検証を chat-main へ依頼する**（#376 の 3 タブ・既存タスクの配置・**既存ノートを選んだときにリンクが Connect / Notes 側に出るか** — ここが #400 で直した経路。**#407 も追加** = 繰り返し「なし」でアイコンが消え、日をまたいでも「新規予定」が復活しないこと）。§7.4 の localhost 集約ポリシーによりこの worktree では実測できない
- **#407 の merge 後クリーンアップをユーザーへ案内済み**（Issue コメント + PR #423 本文）: Routines タブから「新規予定」routine 2 本（`3c4a1f09` / `b15eb258`）を削除。コード修正だけで新規生成は止まるが、routine 本体と生成済み si- 行は UI からの削除が必要
- Epic #290 の残 Step（Step 5 構成再編 / Step 6 カレンダー台帳配線 / Step 7 エディタ拡充）は未起票。Step 5 の子 Issue が無ければ chat-main へ起票依頼を outbox へ
- chat-main へ起票依頼済み（outbox 2026-07-26 の 3 通）: (1) ~~`web/src/notes/NotesView.tsx:291` の lint error（main 由来）~~ = **2026-07-28 時点で解消済み・起票不要** (2) Mobile 月表示で FAB が `mobileSelectedDay` ではなく `anchorDate` に作る (3) 生成直後の楽観行が同期リフェッチで消えると開いたばかりの詳細エディタが閉じる

## 引き継ぎメモ（この worktree で効く事実）

- **`wiki_tag_connections` へのリンクは「FK 先の行が DB にできてから」しか書けない**（2026-07-26 の #376 QA で実測）。作成系フック（`createScheduleItem` / `addNode`）は**楽観的 id を同期で返して裏で書く**ので、直後にリンクを撃つとリンクの INSERT が先に飛んで FK / RLS に弾かれる（どちらの writer も `auth.getUser()` の 1 往復から始まるが、リンク側にはその前置きが無い）。**「ローカル state にある」は FK 先の存在証明にならない** — これは #371 が `shared/src/utils/pendingItemLinks.ts` の冒頭に明文化済みの罠で、`DailyView.flushPendingLinks` が正しい流儀。#376 は `opts.onSaved` / `options.onSaved` で同じ規律を作成経路に通した（undo / redo には渡さない = redo が sync を再実行するため二重付与になる）
- **`shared/` に eslint config は無い**（lint は web だけの script）。`cd shared && npx eslint` は config not found で落ちるが、これは異常ではない
- **PR merge は「数分で来る」前提で動く**（2026-07-26 に **2 回**踏んだ = #393 / #395）。どちらも push 前に merge され、後追いのコミットが PR から漏れた。**レビュー → 修正が想定されるなら、PR を立てる前にレビューまで済ませる**。漏れたら `git checkout -b <new> origin/main` + `git cherry-pick <sha>` で切り出し、**main が動いているのでゲートは必ず回し直す**

- **自分の変更範囲外の型エラーの原因を「増分ビルド」と決めつけない**（2026-07-26 に一度誤診し、role-qa の監査で訂正）。`web/package.json` の build は**最初から `tsc -b --force`** で references 経由の shared をフルチェックしている（`cd web && npx tsc -b --force --dry` で確認可）。同セッションの他ブランチが緑だったのは増分のせいではなく、**分岐元の main がまだ壊れていなかった**だけ（`git merge-base origin/main <branch>` で実測できる）。#378 のような squash merge 事故は**壊れた版がどのブランチにも存在せずマージ後の main にだけ現れる**ので、手元の検証を厳しくしても捕まらない — 穴は「マージ後の main で誰もビルドしない」側にある
- **この PC には project 側 skill-lib の実体が無い**（`.claude/skills/` の中身は Mac パスへのシンボリックリンクがテキストとして checkout されている）。ただし **`task-tracker` はユーザーグローバル（`~/.claude/skills/`）にあり Skill ツールから起動できる**（2026-07-26 実測 — 「全て手動」は誤り）。`session-loader` 等プロジェクト固有スキルだけが不在
- **PR は作った直後に merge されることがある**（2026-07-26 実測）。#393 は作成から数分で merge され、後から push した Step B のコミットは PR に入らなかった。**1 PR に段階を積むつもりなら、全部 push し終えてから PR を立てる**。取り残したら `git checkout -b <new> origin/main` + `git cherry-pick <sha>` で切り出せる（Step A の内容は既に main にあるので衝突は最小 — 実際 `shared/src/index.ts` の隣接追加 1 箇所だけだった）
- ~~**`web` の lint は緑ではない**~~: `web/src/notes/NotesView.tsx:291` の main 由来 error は **2026-07-28 に解消済み**（origin/main `415cb185` で `cd web && npm run lint` が exit 0）。以後 lint 赤が出たら自分の変更を疑ってよい。なおセクションの標準ゲートは shared test / shared build / web build で lint を含まない
- **`REALTIME_TABLES`（`shared/src/context/SyncContext.tsx`）は publication と完全一致が不変式**（`shared/tests/syncRealtimeTables.test.ts` がハードカウント込みで検証）。DDL を伴わないコード削除でテーブルを購読リストから外すとテストが落ちる — #352 で一度踏んだ
- **`web` にテストランナーが無い**（scripts は dev / build / lint / preview のみ）。ホスト側のロジックはテストで守れないので、**判定ロジックは shared の純粋関数 / フックに寄せる**と vitest で pin できる（#352 の `seedFrequencyPatch`、#355 の `useDeferredAction` はこの方針）
