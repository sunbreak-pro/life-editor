# MEMORY (chat-connect-refine)

## 進行中

（なし）

## 直近の完了

- ハブの行タイトルと種別見出しの文字を 1 段大きくした（ユーザー直依頼・Issue なし）✅（2026-09-20・**PR #1790 open**）
- 390 幅で長いタイトルの行が横スクロールを生む問題を直した（#1749）✅（2026-09-19・**PR #1751 merged**）
- タグのアイコンを 56 → 146 個に増やし、選択パネルに日本語で引ける検索欄を足した（#1700 / #1701）✅（2026-09-19・**#1700 = PR #1740 merged**、**#1701 = PR #1743 open**）
- Step 12 のブロッカー 2 件を直した（#1732 選択バーの崩れ / #1733 Mobile のトーストがタブバーに重なる）✅（2026-09-19・**PR #1735 open**）
- Connect ワークベンチの残り 4 Issue を 5 PR で出した（#1676 / #1644 / #1645 / #1646）✅（2026-09-19・**#1676 = PR #1685 merged**、**PR #1696 / #1698 / #1703 / #1709 open = merge 待ち**）。計画書 `2026-09-14-connect-tag-link-workbench.md` は Status COMPLETED で `archive/` へ
- タグ編集モーダルを Connect に畳み、タグマスタの編集所を一本化した（#1643・`D-20260912-main-1` Q1-A）✅（2026-09-16・**PR #1657 open = merge 待ち**）
- 繰り返しアイテムのタグが Connect に出ず、タグ編集で「その他 (無題)」になる問題を直した（#1631）✅（2026-09-16・**PR #1650 merged**）
- narrow の絞り込み入力の箱を `label` にして余白タップでフォーカスが入るようにした（#1578・#1561 の残件）✅（2026-09-09・**PR #1585 merged**）

## 予定

- **PR #1790 の merge 後に chat-main 側で実ブラウザ確認**: ハブ中央列の行タイトルが 14px になってホバーのチェックボックス（16px）と釣り合うか、Note / Todo の見出しが行から浮き上がって見えるか。1440×900 と 390×844 の両方
- **PR #1751（#1749）は merged 済み。chat-main 側で 390 幅の実ブラウザ確認が残り**: 長いタイトルのアイテムを持つタグを開き、横スクロールバーが消えて「…」が画面内に収まるか。jsdom はレイアウトを持たないのでここだけは実機が要る
- **4 本の PR は merge 順が決まっている**（どれも base = main。後ろの PR は前の内容を含む）: #1696 → #1698 → #1703 → #1709。merge はユーザー（P-001）
- **merge 後に chat-main 側で実ブラウザ確認**（計画書 Step 12 = 👀）: 1440×900 で盤面 1a〜2f、390×844 で Mobile 1a〜1l、light / dark 両方。一括操作 10 件の体感（Realtime の再取得が詰まらないか）もここで実測する
- **判断待ち**: Mobile の上部バー「＋」（D-20260919-connect-1）と、Step 12 で出た設計との差分 3 件（D-20260919-connect-2 = #1734）。どちらも `decisions/chat-connect-refine.md`
- **一括操作の体感は chat-main が実測済み**: 10 件でタグ付けが 2,881ms / 外すが 45ms（楽観更新）。押してから 1 秒以上何も動かないので、進捗表示を足すかは判断待ち
- **PR #1657 の merge 後に chat-main 側で実ブラウザ確認**（CLAUDE.md §7.4 — worktree は build / 型検証まで）: 1440×900 で盤面 1a〜2f、light / dark 両方。特に編集ブロックの色スウォッチ 2 段 6 列とレールの「…」ホバー表示
- chat-main 側で 390 幅の実測（PR #1585 merged 済み）: 絞り込み枠の上端 4px 下をタップして `document.activeElement === input` になるか
- **PR #1743（#1701）の merge 後に chat-main 側で実ブラウザ確認**: アイコンパネルの見た目（8 列 19 行のスクロール）・実際の日本語入力での絞り込み・↑ ↓ と Enter。jsdom では座標も IME 実体も無いので、ここだけは実機が要る
- **アイコン 2 本で eager チャンクが合計 +17.4 KB gzip 増えた**（#1366 の予算はアイコンぶん +15 KB）。重いと判断されたら `tagIconAliases.ts` を `import()` で遅延するのが一番安い削り方（パネルを開くまで誰も読まないため）。判断は保留中で Issue は立てていない
- `web/tests/briefingEveningLazyMount.test.tsx` のフル実行時 flake 起票（outbox に投函済み・chat-main 待ち）
