# MEMORY (chat-connect-refine)

## 進行中

### 🔧 #181 connect 行 — Layout Standard v1 adoption（着手日: 2026-07-11）

**対象**: `shared/src/components/Connect/ConnectHeader.tsx`, `shared/src/components/Connect/ConnectGraphView.tsx`
**計画書**: `.claude/docs/vision/plans/2026-07-11-connect-refine-orders.md`

- 前回: —
- 現在: 実装・検証完了（fluid variant 採用確認 + rem gutter → px lumen gutter トークン置換。shared build + 768 tests + web build pass・role-qa PASS）。commit → push → PR 作成 → #181 コメントへ
- 次: PR merge 後に #181 の connect 行チェック + orders 計画 Status 更新

## 直近の完了

（なし）

## 予定

- Layout Standard v2 adoption（v2 共通部品の main merge 待ち — orders 計画参照）
- life-tags 統一計画 Step 2 のレビュー参加（合図待ち）
