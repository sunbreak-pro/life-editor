# MEMORY (chat-mobile-refine)

## 進行中

- **#1290 Mobile のタグ編集導線** → **PR #1308 open**（2026-08-30 時点。`origin/main` 直上・CI 実行中）。narrow の「その他」シートに「タグを編集」行を足しただけで、**エディタ内部（`tagEdit/*` / `TagEditorHost.tsx`）は差分ゼロ** — #1289（アイコンピッカーの背景透過）が別レーンで同じ内部を直しているため。#1289 が先に着地したら rebase で追随する。merge 後の実機確認（390px の導線 / ソフトキーボードと中央固定モーダルの相性）は outbox で chat-main に依頼済み

## 判断待ち（回答が付いたら消化 → 台帳へ昇格）

- （なし）

## 直近の完了

- **#1050 モバイルドロワーのスライド + エッジスワイプ ✅**（2026-08-18・PR #1074 merged）
- **#1049 セクション初回表示の fade-in ✅**（2026-08-18・PR #1069 merged）
- **#1035 narrow 全セクションヘッダーの Undo/Redo ✅**（2026-08-18・PR #1066 merged）

## 予定

- **#1035 の積み残し**: `mobile-scope.md` の #16 行が「Undo/Redo の導線 = その他シート」のまま。全セクションのヘッダーにも出るようになったので追随が要る（#1290 の PR #1308 でも同ファイルを触ったが、スコープ外なので混ぜていない — #1308 の着地後に小 PR で）
- **merge 済み 5 本（#1014 / #1039 / #1035 / #1049 / #1050）の実ブラウザ確認を chat-main へ依頼する**（このレーンは playwright を使えない = CLAUDE.md §7.4）。特に #1039 の 44px オーバーレイ（`TAP_TARGET_TALL`）は「見えない当たり判定が隣を食っていないか」を実機で見ないと確証が持てない
- #691 の残件 = Issue **#761**（Dayflow の Todo 行から完了・詳細へ）は `web/src/schedule/**` = schedule-refine の担当レーン
