---
Status: IN PROGRESS # 2026-08-06 Phase 1（計測）/ 2（枠づくり）/ 4（維持機構）を実施。残 = Phase 3（移送）— 移行完了まで着手不可
Created: 2026-08-04
Branch: docs/context-cost-harness # 実装セッションのブランチ（配置 PR は docs/loop-harness-phase1）
Owner-chat: main
Parent: .claude/docs/vision/plans/2026-07-28-loop-engineering-harness.md
---

# Plan: コンテキストコスト削減ハーネス — 測って、減らして、戻らない仕組みを作る

> **これは何か**: セッションごとに固定で乗るコンテキスト費用を実測し、削減し、**再肥大しない機構**を作るための調査 + 実装計画。
> **主目的は削減そのものではなくハーネスの構築**。一度削っても維持機構がなければ数か月で戻るため、計測・棚卸し・移送を回し続ける仕組みを最終成果物とする。
> **姉妹計画**: ループ定義の構造は `2026-08-04-loop-catalog.md` が扱う。本書はそこから分離したコスト系の全項目を引き受ける。

---

## Context

### 動機

1. **固定費が見えていない** — セッション開始時点で何がどれだけ積まれているかを誰も測っていない。CLAUDE.md だけが議論の対象になりがちだが、実際の支配項は別の可能性が高い。
2. **削減が一過性で終わる** — 「変わらない事実だけを持つ」方針で運用してきたにもかかわらず、手順・経緯・実測エピソードが再び混ざる。**方針だけでは戻る。**
3. **判断材料がない** — 何を残し何を移すかを、感覚ではなく費用対効果で決めたい。

### 仮説（調査で検証する)

- CLAUDE.md 本体よりも、**そこから参照される大型 SSOT の読み込み**の方が重い可能性がある
- **MCP サーバーのツール定義**は毎セッション積まれるため、使用頻度の低いサーバーが常時費用になっている可能性がある
- path-scoped の詳細規約は、**実際の発火頻度**次第で常時費用に近い扱いになっている可能性がある
- SessionStart hook の出力・派生 INDEX の再生成結果も、毎回コンテキストに入るなら固定費である

いずれも**未実測**。本計画の Phase 1 はこの検証だけを目的とする。

### 制約

- 移行期間中（Electron + Capacitor + Web + Supabase）。**本格的な移送は移行完了後**に行う（2026-08-04 裁定）。移行中は計測と枠づくりに留める
- 消失ゼロ — 削除は移送先の確保とセットで行う。移送先が無い記述は、移送先を作るまで消さない
- 既存ゲート（lint / build / test / docs-lint）を壊さない
- $0 運用

### Non-goals

- 削減量そのものを KPI にすること（削りすぎは探索コストを増やし逆効果）
- ループ定義の構造設計（→ 姉妹計画）
- モデル選択・思考量チューニングによる削減（本計画はコンテキスト側に限定）

---

## 1. ハーネスの全体像

```
[計測]   固定費を定期的に測る            ← これが無いと何も判断できない
   │
[判断軸] どこに置くべきかを表で決める    ← 感覚で決めない
   │
[移送]   常時 → 条件付き / 参照 へ移す   ← 削除ではなく移送
   │
[維持]   /loop-prune で棚卸しを回す      ← 戻らないための機構（最終成果物）
```

削減は 3 段目で起きるが、**成果物として残すのは 1 段目と 4 段目**である。

---

## 2. 判断軸

### 2.1 その記述をどこに置くか

| 性質                                   | 置き場                 | 例                                               |
| -------------------------------------- | ---------------------- | ------------------------------------------------ |
| 常に必要で、モデルが知りようがない事実 | CLAUDE.md 航法         | ディレクトリの責務・検証コマンド・踏むと壊れる罠 |
| 判断の優先順位・プロダクトの意図       | CLAUDE.md 目的         | Vision・Non-Goals・優先順位                      |
| 特定の作業をする時だけ要る手順         | skills/                | worktree 操作・DB migration・Issue 起票          |
| 特定パスを触る時だけ要る規約           | rules/（path-scoped）  | frontend 規約                                    |
| 破られたら事故になる禁止               | hooks / permissions    | main への push・トークン平文                     |
| 経緯・実測・失敗談                     | docs/ ・ known-issues/ | なぜその決定になったか                           |

判定式は **「消したら Claude が間違うか」+「毎回必要か」**。毎回必要でないなら常時ロードから外す。

### 2.2 削ってよいか

| 状態                             | 判断                           |
| -------------------------------- | ------------------------------ |
| 移送先が既にある                 | 移送して削る                   |
| 移送先が無いが作れる             | 移送先を作ってから削る         |
| 移送先が無く、作る先も決まらない | **残す**（削減を目的化しない） |
| モデルが推論で埋められる一般知識 | 削る                           |
| 環境固有の事実                   | 削らない                       |

### 2.3 常時ロードの再編（航法 / 目的の 2 層）

CLAUDE.md は最終的に次の 2 層のみを持つ形へ再編する。

- **A. 航法（Navigation）** — 探索コストを下げる「事実」だけ。構造・参照先マップ・検証コマンド・既知の罠
- **B. 目的（Why）** — 判断を委ねるための「基準」。Vision・Non-Goals・優先順位

全面書き換えではなく **既存記述の振り分け**として行う。手順は skill へ、経緯は docs へ、禁止は hooks / permissions の実体へ。実体が無い禁止は、実体を作ってから文章を削る。

---

## 3. Phase 構成

```
Phase 1  計測          ← 今ここ。移行中でも実施できる
  │  固定費の内訳を測り、支配項を特定する。この時点では何も削らない
  │  昇格条件: 内訳が数字で出ており、上位項目が特定できている
  ▼
Phase 2  枠づくり
  │  航法 / 目的の枠を CLAUDE.md に用意し、移送先（skill / docs）を先に作る
  │  昇格条件: 移送先が揃い、消失ゼロで移送できる状態
  ▼
Phase 3  移送          ← 移行（Electron + Supabase）完了後に実施
  │  Phase 1 の上位項目から順に移送する。1 回の PR で 1 項目
  │  昇格条件: 既存ゲートが緑・再測定で固定費が下がっている
  ▼
Phase 4  維持機構
     /loop-prune を定義し、定期的な棚卸しを回す。カタログ自身も対象に含める
```

---

## 4. Phase 1 の調査項目

**何も削らず、内訳を出すことだけを目的とする。**

| 対象                               | 見るもの                                                             |
| ---------------------------------- | -------------------------------------------------------------------- |
| CLAUDE.md                          | 実サイズと、そこから参照されるファイルの追跡実態                     |
| **グローバル資産（`~/.claude/`）** | **常時ロードされる CLAUDE.md / rules / output style / 一覧の説明文** |
| 大型 SSOT（移行 SSOT 等）          | 実際に何回・どの範囲まで読まれているか                               |
| MCP サーバー                       | 接続中サーバーのツール定義がセッション開始時に占める量と、実使用頻度 |
| path-scoped rules                  | 発火条件と実際の発火率                                               |
| SessionStart hook                  | 出力がコンテキストに入るか、入るなら量                               |
| skills の説明文                    | 全 skill の説明文が常時読まれる前提での合計量                        |
| memory / history                   | セッション開始時に読まれる範囲                                       |

> グローバル資産の行は **2026-08-06 の実測で追加**（起草時は表に無かった）。固定費の約 66% がここにあり、単一の支配項だったため。実体は別リポジトリ `claude-dotfiles` で本書の Scope 外 — 入れるかは D-20260806-main-2。

調査結果は本書の Worklog ではなく、**独立した計測結果ファイルに残す**（再測定して差分を見るため）。**結果 = [`../../reports/2026-08-06-context-fixed-cost-baseline.md`](../../reports/2026-08-06-context-fixed-cost-baseline.md)**（計測スクリプト = `.claude/scripts/context-cost-measure.mjs`）。

---

## Scope (Touchable Paths)

```
.claude/CLAUDE.md
.claude/skills/**
.claude/rules/**
.claude/docs/**
.claude/scripts/**
.claude/settings.json
.mcp.json
.claude/docs/vision/plans/<this-file>.md
```

**グローバル資産は 2026-08-06 に Scope へ追加**（D-20260806-main-2 = A）。実体は別リポジトリなので **PR も別**になる:

```
<claude-dotfiles>/claude/CLAUDE.md
<claude-dotfiles>/claude/rules/**
<claude-dotfiles>/claude/skills/**
<claude-dotfiles>/claude/output-styles/**
```

Phase 1 の間は **読み取りと計測結果ファイルの追加のみ**。既存ファイルは変更しない。

プロダクトコード（`shared/` `web/` `desktop/` `mobile/` `mcp-server/` `supabase/`）には触れない。

---

## Steps

| #   | Step                                          | Gate    | Acceptance                                                                                                           |
| --- | --------------------------------------------- | ------- | -------------------------------------------------------------------------------------------------------------------- |
| 1   | 計測手段の確定（何をどう測るか）              | 👀 目視 | 再現可能な手順が書かれている — **完了**（`.claude/scripts/context-cost-measure.mjs` + 計測結果 §6）                  |
| 2   | Phase 1 調査の実施（概算 → 上位項目のみ精密） | 🤖 自律 | 内訳が数字で出ており、上位項目が特定できる — **完了**（計測結果 §2 / §3）                                            |
| 3   | 調査結果のレビューと削減方針の裁定            | 🛑 人手 | 移送対象と順序が決まる — **判断待ち**（D-20260806-main-2 / -3）                                                      |
| 4   | Phase 2 枠づくり（移送先の作成）              | 🤖 自律 | 移送先が揃い、既存ゲートが緑 — **完了**（`docs/vision/claude-md-layering.md`。新規作成が必要な移送先は 0 件だった）  |
| 5   | Phase 3 移送（移行完了後・1 PR = 1 項目）     | 🤖 自律 | 再測定で固定費が下がり、消失ゼロ — **未着手**（移行完了が着手条件。前倒し可否 = D-20260806-main-3）                  |
| 6   | Phase 4 `/loop-prune` 定義と定期実行          | 👀 目視 | 棚卸しが 1 周回り、検出結果が妥当 — **定義のみ完了**（`.claude/skills/loop-prune/SKILL.md`。1 周目の実運用は未実施） |

---

## Acceptance Criteria (機械検証可能)

- [x] 計測結果ファイルが存在し、再測定手順が書かれている（`docs/reports/2026-08-06-context-fixed-cost-baseline.md` §6）
- [ ] 移送によって削除した記述が、移送先ファイルに存在する（消失ゼロ）— **グローバル側は満たした**（docs 構造テンプレ → `project-setter` スキルへ全文移送）。**life-editor 側は Phase 3 待ちで削除 0 件**
- [ ] CLAUDE.md が航法 / 目的の 2 層構造になっている — **枠のみ完了**。振り分け表（`docs/vision/claude-md-layering.md` §2）と CLAUDE.md §0 のポインタは置いたが、**本文の再編は Phase 3**
- [x] `.claude/skills/loop-prune/SKILL.md` が存在し、必須 5 見出しを満たす
- [ ] 移送前後の再測定で固定費が減少している — **グローバル側のみ達成**（-1,182 tok = `claude-dotfiles#13`・merge 前の見込み値）。**life-editor 側は満たせない** — 移送（Phase 3）が移行完了待ちのため、比較対象の「後」が存在しない
- [x] 既存の lint / build / test / docs-lint ゲートがすべて緑（docs のみの変更のため docs-lint + prettier）
- [x] 完了時に本 plan と per-chat memory の Status を更新した（**COMPLETED にはしない** — 残が Phase 3 のみ）

---

## 確定事項（2026-08-04 裁定）

- **計測は二段構え** — まず概算で上位項目だけを特定し、そこだけ精密に測る。全項目の精密計測はしない（調査自体のコストを抑えるため）
- **削減の数値目標は置かない** — 基準は「移送先があるものは移す」のみ。固定費の上限値を運用ルールにしない（削りすぎの防止）
- **大型 SSOT の分割可否は Phase 1 の結果を見てから決める** — 移行中の分割は事故りやすいため、先に方針を固定しない

## 確定事項（2026-08-06 裁定）

- **グローバル資産（`~/.claude/` = `claude-dotfiles`）を Scope に入れる**（D-20260806-main-2 = A）。固定費の約 66% がそこにあり、プロジェクト側だけでは効く上限が 3 分の 1 だったため。**PR は別リポジトリ側**で立てる。ただし**削るのは「保険」の 1 層に絞る** — 口調の三重化は意図的な設計で、正本（output style）と詳細版（`rules/tone.md`）には手を付けない
- **Phase 3 の移行ゲートは維持する**（D-20260806-main-3 = A）。life-editor 側の移送は移行完了後。**グローバル側はこのゲートに縛られない** — ゲートの根拠は「移送先自体が動く」ことであり、移行で動くのは life-editor の CLAUDE.md §2〜§5 と移行 SSOT であって `~/.claude/` ではないため

---

## Risks

- **削りすぎ** — 「モデルは分かるはず」で環境固有の事実まで削ると、探索が増えて逆に高くつく。2.2 の判定表を必ず通す
- **移行との衝突** — 移行中に移送すると、移送先自体が動く。Phase 3 を移行完了まで開始しない
- **調査が目的化する** — 計測に時間をかけすぎない。上位項目が見えた時点で次へ進む
- **維持機構の未完** — Phase 4 まで到達しないと、削減は数か月で元に戻る。**Phase 3 単独での完了扱いを禁止する**

---

## References

- 親計画: `.claude/docs/vision/plans/2026-07-28-loop-engineering-harness.md`
- 姉妹計画: `.claude/docs/vision/plans/2026-08-04-loop-catalog.md`
- 現行の常時ロード: `.claude/CLAUDE.md`・`.claude/rules/`
- MCP 構成: `.mcp.json`・`.claude/settings.local.json`（git 非追跡）
- ドキュメント運用: `docs-workflow` スキル（正本）

---

## Worklog

- 2026-08-06: [chat-main] **Phase 1 / 2 / 4 を実施**（Phase 3 は移行完了待ちのため未着手 — Status は `IN PROGRESS` のまま）。成果物 = 計測スクリプト `.claude/scripts/context-cost-measure.mjs` / 計測結果 `docs/reports/2026-08-06-context-fixed-cost-baseline.md` / 振り分け表 `docs/vision/claude-md-layering.md` / `.claude/skills/loop-prune/SKILL.md`。**実測で前提が 3 か所崩れた**: ① 支配項は CLAUDE.md ではなく**グローバル資産**で、固定費 19,277 tok の約 66%（12,711 tok）が `~/.claude/`（別リポジトリ）にある。§4 の調査表に行を追加した ② **口調定義が 3 か所で重複**し、単一項目としては最大の 4,472 tok（全体の 23%）。output style / `rules/tone.md` / `~/.claude/CLAUDE.md` 口調章が「正本 / 詳細版 / 保険」を自己申告しており、事故ではなく意図的な冗長化 ③ **SessionStart hook の出力は 0 バイト**だったが、理由が異常 — `session-start-check.sh` が Windows で `stat -f` の BSD / GNU 差により exit 1 しており、`.session-name` / dirty worktree の検査が 1 つも走っていない（実体は `~/dev/Claude/hooks-lib/` = Scope 外・修正の当たりは 1 行）。**Phase 2 の照合結果**: 新規に作らないといけない移送先は **0 件**（全部実在）。作る必要があるのは「実体の無い禁止」7 件の機械実体だけで、これは移送と同一 PR で 1 つずつ入れる。**判断 2 件をキューへ**: グローバル資産を Scope に入れるか（D-20260806-main-2）/ Phase 3 の移行ゲートを前倒しで開けるか（D-20260806-main-3）。**カタログが 5 本目に到達**した件は姉妹計画側の Risks を更新（4 本上限は「`loop-prune` が来るまで」の条件付きだったため、本ループの追加でその条件が満たされた）
- 2026-08-07: [chat-main] **`/loop-prune` 第 1 回**（対象 = `~/.claude/rules/tone.md`）。**反復 5 / 所要 約 25 分**（上限 5 回・30 分）で 2,184 → 1,458 tok（**-726**）。**当初の -1,500 案は実測で棄却**した — プローブ用サブエージェントに自分の指示文の内容を引用させたところ、**output style はメイン会話にしか届かず、`rules/` と global CLAUDE.md は届く**と判明し、`tone.md` がサブエージェントにとって唯一の口調定義だったため（記録 = 計測結果 §8）。落としたのは output style に同等の記述がある 5 節のみで、比喩の具体例・ユーザー本人の話し方・良い例 / 悪い例（output style が明示委譲している先）は残した。グローバル側の累計は **-1,945 tok**（見込み固定費 19,463 → 約 17,520）。**ループ運用の知見 2 件**を skill の `## 環境の事実` に追加: ①「重複しているから落とす」の前に**誰に届いているか**を確かめる ②置いたばかりのスキルは merge + セッション再起動まで `Skill` から起動できない（1 周目は手で回した）
- 2026-08-06: [chat-main] **判断 2 件に回答（どちらも A）→ グローバル側の第 1 回移送を実施**。D-20260806-main-2 = A でグローバル資産を Scope へ追加し、`claude-dotfiles#13` で **-1,182 tok**（`~/.claude/CLAUDE.md` 1,991 → 678・`project-setter` の一覧行 +131）。撤去したのは**口調の「保険」1 層のみ**で、正本の output style と詳細版 `rules/tone.md` は無傷。docs 構造テンプレは `project-setter` スキルへ全文移送した — **同スキルは 2 か所（`~/.claude/CLAUDE.md` と `code-teacher`）から参照されていたのに実在しない死んだポインタだった**ので、あわせて解消。D-20260806-main-3 = A で life-editor 側の移行ゲートは維持（**グローバル側はゲートの根拠「移送先が動く」に当たらないため対象外**と整理）。計測スクリプトに `CLAUDE_GLOBAL_DIR` を追加し、merge 前の変更を先に測れるようにした
- 2026-08-04: plans/ へ配置（chat-main・配置 PR = `docs/loop-harness-phase1`）。実施順序はループカタログの後で確定（ユーザー裁定）。計測時の注意を 1 点申し送り: 現行 Claude Code には deferred tools（ツール定義を必要時に取り寄せる遅延ロード）があり、「MCP ツール定義が毎セッション全量積まれる」仮説は環境により部分解消されている可能性がある — Phase 1 で前提ごと実測すること
