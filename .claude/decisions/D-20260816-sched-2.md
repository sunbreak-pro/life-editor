---
id: D-20260816-sched-2
type: decision
status: answered
asked: 2026-08-16
answered: 2026-08-16
chat: schedule-refine
answer: A
topics: [schedule, refactoring, testing, props]
refs:
  [
    "#893",
    "#936",
    "shared/tests/weekTimeGrid.test.tsx",
    "shared/tests/eventEditorPane.test.tsx",
    "shared/tests/itemCreatePanel.test.tsx",
  ]
supersedes: []
superseded-by: []
implemented-by: ["#936"]
promoted-to: null
---

# D-20260816-sched-2: オブジェクト prop へ畳んだ部品のテストは「ケース本体を変えずファクトリだけ畳む」でよいか

## 背景

（キュー原文 = `comm/decisions/chat-schedule-refine.md`）

- 背景: Issue #893 / PR #936 / `shared/tests/{weekTimeGrid,eventEditorPane,itemCreatePanel,...}.test.tsx`。Issue の DoD が「既存の `weekTimeGrid.test.tsx` ほかが**無改変**で緑」と書いているが、props の形を変える以上 `<WeekTimeGrid weekStart={...}>` と書かれた行はテスト側でも必ず書き換わる。**DoD が内部で矛盾している**ので、止めずに次に強い形を選んで進めた（止めると Issue 全体が着手不能になるため）
- A: **採用した形（推奨）** = `it(...)` の中身・アサーション・操作手順は 1 行も変えず、各テストファイル冒頭の render ファクトリだけが束ね直しを引き受ける。ケースは `renderPane(manualItem, { canEditDate: true })` のようにフラットなまま。「同じケース・同じアサーションが、部品だけ差し替えて緑」が挙動変更ゼロの根拠になる。代償はファクトリが 1 枚のシムになり、テストを読んだだけでは実 API の形が見えないこと
- B: テストも新 API の形で書き直す（ケース本体も `renderPane` を通さず `handlers={{...}}` を直に書く）。API がテストから読めるようになる代わりに、**アサーション以外の全行が動くので「無改変で緑」による挙動変更ゼロの担保が消える**
- 放置時: A のまま（PR #936 は現状で完結している）
- 期限感: **#889（CalendarTab 分割）の前に決まると嬉しい** — 同じ判断を繰り返すため

## 選択肢と裁定

- A: **ケース本体を変えずファクトリだけ畳む**（**採用** — ユーザー回答 2026-08-16）。理由: props を束ねるリファクタで守りたいのは「見た目と挙動が 1 ミリも変わっていない」ことであり、その証明は**同じケース・同じアサーション・同じ操作手順が部品の差し替えだけで緑になる**という形でしか取れない。ファクトリが変換シムになって実 API の形がテストから読みにくくなるのは事実だが、それは部品側の型定義を読めば分かることで、失う担保のほうが高くつく
- B: テストも新 API の形で書き直す（却下 — アサーション以外の全行が動くため、緑になっても「挙動が変わっていない」の根拠にならない。API の可読性は型定義が担う）

## 却下案が復活する条件

- 束ね直しが完全に終わり、**挙動変更ゼロの担保がもう要らなくなった後**なら、ファクトリを剥がして実 API 直書きに寄せてよい（機械的な置換で足りる別 PR）。担保が要る期間中の書き直しだけを禁じている

## 波及

- **#889（CalendarTab 分割）以降の同種リファクタも同じ形で進める** — 部品の prop / 引数の形を変える PR では、テストのケース本体を動かさずファイル冒頭のファクトリだけで吸収する
- PR #936 は現状（A の形）で完結。追加の書き換えは不要
