---
id: D-20260815-briefing-1
type: decision
status: answered
asked: 2026-08-15
answered: 2026-08-16
chat: briefing-refine
answer: A
topics: [briefing, goals, data-model, notes]
refs:
  [
    "#872",
    "shared/src/components/briefing/dailySections.ts",
    "web/src/briefing/hooks/useDailySections.ts",
  ]
supersedes: []
superseded-by: []
implemented-by: ["#914"]
promoted-to: null
---

# D-20260815-briefing-1: #872 の目標をどこに保存するか

## 背景

（キュー原文 = `comm/decisions/chat-briefing-refine.md` をそのまま貼る）

- 背景: Issue #872（週・月・年の目標を Briefing に常設表示）。実測 = 「宣言」が既に TipTap 見出しをセクション扱いして DDL ゼロで書き込み UI を成立させている（`shared/src/components/briefing/dailySections.ts` + `web/src/briefing/hooks/useDailySections.ts`）
- A: 専用ノート 1 枚の本文に `## 週目標` / `## 月目標` / `## 年目標` を持つ（推奨 — DDL ゼロ・宣言の実装をそのまま写せる・PR 1 本で閉じる。副作用として Notes 側でも同じ目標を編集できる）
- B: 新規テーブル（`items_meta` + `goals_payload` の 2 行分割）。データモデルは綺麗だが DDL = 🛑 こうだいさんの `supabase db push` が挟まり PR 1 本で閉じない。mapper / RLS / Realtime / `syncDomains.ts` 対応表 / lockstep テストまで芋づるで増える
- 放置時: A で進める（後から B へ移送可能。移送は「本文をパースして行を作る」1 回の migration で済む）
- 期限感: #872 の実装開始まで（回答が無ければ A で着手する）

## 選択肢と裁定

- A: 専用ノート 1 枚の本文に見出しでセクションを持つ（**採用** — ユーザー回答 2026-08-16。DDL ゼロで PR 1 本に収まり、既に動いている「宣言」の実装をそのまま写せる）
- B: `items_meta` + `goals_payload` の新テーブル（却下 — DDL push が 🛑 人手ゲートになり PR 1 本で閉じない。mapper / RLS / Realtime / lockstep テストまで芋づるで増える）

## 却下案が復活する条件

目標に「型付きフィールド + 集計」（達成率・期間比較など）が要るようになったとき。本文パース 1 回の migration で B へ移送できる形を保っている。

## 波及

- 実装 = #872（PR #914 着地済み）。保存の器 = `shared/src/components/briefing/goalSections.ts`
- 副作用として Notes 画面からも同じ目標を編集できる（受容済み）
- 期間ロールオーバーの扱いは別判断 = [`D-20260815-briefing-3`](./D-20260815-briefing-3.md)
