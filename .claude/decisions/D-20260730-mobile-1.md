---
id: D-20260730-mobile-1
type: decision
status: recorded
asked: 2026-07-30
answered: 2026-08-01
chat: mobile-refine
answer: A
topics: [mobile, ux, touch]
refs: []
supersedes: []
superseded-by: []
implemented-by: ["#494"]
promoted-to: null
---

# D-20260730-mobile-1: モバイルの 3 択タッチ行を維持するか

> キュー原文は消化済み — `ANSWERS.md` の行から再構成（2026-08-09 台帳移行）。**「放置時 A」での確定**であり、ユーザーの明示回答ではない — 2026-08-01 の回答は mobile-2 / mobile-3 のみを指名した（chat-main 記録）。キューの「放置時 = 安全側」設計が意図どおり機能した実例。

## 裁定

**A: 3 択のタッチ行を維持**（放置時の挙動として確定）。

## 実装の着地（2026-08-12 実測 — Epic #716）

3 択のタッチ行は `TaskStatusChoices`（Issue #470 / PR #494）として現存し、Mobile の詳細シートにだけ入っている。裁定は「維持」なので、行を導入した PR がそのまま着地点。

- 部品 = `shared/src/components/TaskStatusChoices.tsx:41-72` — `STATUS_ORDER` の 3 状態を `grid-cols-3` で横並びにし、各ボタンは `min-h-11`（44px のタッチ target）で 1 タップ確定
- Mobile 配線 = `web/src/tasks/KanbanView.tsx:736-746` — `TaskDetailPanel` の `statusControl` スロットへ注入。Desktop（`KanbanView.tsx:731`）はスロット無しで巡回ボタンのまま
- スロットの加算性 = `shared/src/components/TaskDetailPanel.tsx:219`（`statusControl ?? <巡回ボタン>`）
- 守り = `shared/tests/taskStatusChoices.test.tsx`
