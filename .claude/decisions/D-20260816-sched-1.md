---
id: D-20260816-sched-1
type: decision
status: answered
asked: 2026-08-16
answered: 2026-08-16
chat: schedule-refine
answer: A
topics: [schedule, refactoring, abstraction, supabase-service]
refs: ["#897", "#929", "shared/src/services/SupabaseScheduleItemsService.ts"]
supersedes: []
superseded-by: []
implemented-by: ["#929"]
promoted-to: null
---

# D-20260816-sched-1: #897 のステップ 2「bulkCreate と updateFuture の共通部分を切り出す」を見送ってよいか

## 背景

（キュー原文 = `comm/decisions/chat-schedule-refine.md`）

- 背景: Issue #897 / PR #929 / `shared/src/services/SupabaseScheduleItemsService.ts`。Issue 本文が挙げた共通部分 3 つを実測したところ、共通の中核が実在しなかった —「ルーチン由来の行の絞り込み」は bulkCreate が**これから INSERT する行へのメモリ上の述語**・updateFuture が**1 routine への DB クエリ**で同名の別物、「2 行分割の組み立て」は bulkCreate が既に共有 `scheduleItemToRows` を使い updateFuture はそもそも行を組み立てない、「冪等化オプション」は bulkCreate 専用
- A: **見送る（推奨 — 呼び出し元が 1 つずつの間接層を足すだけになり、Issue が減らしたい行数はむしろ増えるため）**。代わりに bulkCreate 内部の `fetchLiveRoutinePairKeys` / `routinePairKey` 切り出しに留める（PR #929 の現状）
- B: またぐ抽象を入れる（2 メソッドの外側に「ルーチン由来行の解決 → 2 行分割の書き込み」の共通レイヤを新設し、両方をその上に載せ替える）
- 放置時: A のまま（PR #929 は現状で完結している）。B を採るなら別 Issue を起票して独立 PR で行う
- 期限感: いつでも（#929 の merge をブロックしない）

## 選択肢と裁定

- A: **見送る**（**採用** — ユーザー回答 2026-08-16）。理由: Issue #897 が挙げた「共通部分」3 つは名前が同じだけで中身が別物だった。同名 = 共通とみなして抽象を被せると、両側の呼び出し元が 1 つずつしか無い間接層が増えるだけで、Issue が減らしたかった行数はむしろ増える。切り出しは実際に重複していた bulkCreate 内部の `fetchLiveRoutinePairKeys` / `routinePairKey` に留める
- B: またぐ抽象を入れる（却下 — 「ルーチン由来行の解決」がメモリ上の述語と DB クエリという別の操作なので、共通レイヤは両者の差を引数フラグで吸収する形にしかならない。呼び出し元 2 つのために分岐つきの中間層を維持するコストのほうが高い）

## 却下案が復活する条件

- 3 つ目以降の呼び出し元（別の一括書き込み経路）が現れ、「ルーチン由来行の解決 → 2 行分割の書き込み」が実際に 3 箇所で重複したとき。そのときは B を独立 Issue として起票して進める

## 波及

- Issue #897 のステップ 2 は着手しない。#897 はステップ 1（テストによる bulk write 経路の固定 = PR #929）で完結扱い
