---
id: D-20260810-main-3
type: decision
status: answered
asked: 2026-08-10
answered: 2026-08-11
chat: main
answer: A
topics: [skills, vendoring, stale-docs]
refs:
  [
    "D-20260810-main-2",
    "docs/known-issues/031-mac-only-symlinked-skills-agents.md",
    ".claude/skills/db-migration/SKILL.md",
  ]
supersedes: []
superseded-by: []
implemented-by: []
promoted-to: null
---

# D-20260810-main-3: STALE な旧アーキ由来スキル 5 本を書き直すか retire するか

## 背景

vendor 化（D-20260810-main-2）で repo に取り込んだ 8 本のうち **5 本が現行アーキ未追随**と判明した。`db-migration` は SQLite `PRAGMA user_version` + `electron/database/` 前提（実体は Supabase Postgres + `supabase/migrations/`）、`add-ipc-channel` は実在しない Electron / Tauri IPC 層の手順（同じ層の `-ipc-validator` は D-20260708-main-1 で retire 済み）、`add-component` / `add-feature` / `test-writing` は削除済みの `frontend/` ツリーと存在しないテストヘルパ（`renderWithProviders` / `mockDataService` は repo 内 0 件）を指す。repo 外にあったため PR レビューを一度も通っていない。**symlink 時代は「読めない = 無害な空振り」だったが、実ファイル化で「読めて、間違っている」に変わる**ため、当座は冒頭 STALE バナーで発火を止めてある。

## 選択肢と裁定

- A: **5 本とも現行アーキで書き直す**（**採用** — ユーザー回答 2026-08-11。`add-component` / `test-writing` は `shared/src/components/` と `shared/tests/` の実物に倣うだけなので比較的軽い。`db-migration` は `db-conventions.md` §10 と CLAUDE.md §7.3 の内容と重複するので、スキルは薄い導線に絞る形もあり）
- B: 5 本とも retire（削除）する（却下 — `lead-pipeline` + `rules/frontend.md` + `db-conventions.md` + CLAUDE.md §7.1 で実質カバーできてはいるが、「手順の型」を失う）
- C: 分割（`add-ipc-channel` のみ retire・残り 4 本を書き直す）（却下 — A に包含される。ただし `add-ipc-channel` の書き直し先が「IPC 層は存在しない / DataService 境界が正本」という**空振りを止めるための短い導線**になるのは許容）

## 却下案が復活する条件

書き直しの過程で、あるスキルの中身が既存の rules / CLAUDE.md の再掲にしかならないと分かった場合は、その 1 本だけ B（retire）に落とす。

## 波及

- 5 本の SKILL.md を現行アーキで書き直し、冒頭の STALE バナーを外す
- `add-ipc-channel` は「IPC 層は無い・データアクセスの正本は CLAUDE.md §3.1 の DataService 境界」への導線に絞る方向で検討する
- 書き直しは PR レビューを通す（vendor 化前に一度も通っていないため）
