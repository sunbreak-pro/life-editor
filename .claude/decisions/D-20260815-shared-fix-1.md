---
id: D-20260815-shared-fix-1
type: decision
status: answered
asked: 2026-08-15
answered: 2026-08-16
chat: chat-shared-fix
answer: A
topics: [mobile, schedule, calendar, ui-parity]
refs: ["#916", "#917", "#874", "#878"]
supersedes: []
superseded-by: []
implemented-by: ["#916", "#917"]
promoted-to: null
---

# D-20260815-shared-fix-1: #916（月シート廃止）を通すか。通さないなら月シートの全画面化を別途起票する

## 背景

（キューのエントリ本文をそのまま貼る）

- 背景: #874 の対応（PR #917）で Mobile の詳細・編集パネルを全画面化した。`CalendarTab` の月シートだけは外している — #916 が月シートを機能ごと削除する PR で、同じブロックに触ると確実にコンフリクトし、消える予定のブロックへの作業になるため。#874 の DoD 自体は `AppShell` 側の修正（タブバーを unmount せず `invisible` に）で月シートについても満たされる
- A: #916 を通す（推奨 — 月シートが無くなるので #917 の見送りがそのまま正解になり、追加作業ゼロ）
- B: #916 を見送り月シートを残す → 月シートの全画面化を別 Issue で起票する（他の詳細系と見た目が揃わないため）
- 放置時: 現状維持。#917 は月シート未変更なのでどちらの順で merge してもコンフリクトしない
- 期限感: #916 の merge 判断時

## 選択肢と裁定

- **A: #916 を通す**（**採用** — ユーザー回答 2026-08-16）。月シートそのものが無くなるので、#917 が月シートを触らなかった判断がそのまま正解になり、追加作業はゼロで済む
- B: #916 を見送り月シートを残す → 月シートの全画面化を別 Issue で起票（却下 — 消す予定のブロックに全画面化を後付けする作業が丸ごと増える）

## 却下案が復活する条件

- Mobile の月表示を「メインビュー」ではなく再びシート形式で出したくなった場合（#878 の設計をひっくり返すことになるので、その時は月シート復活側の Issue から始める）

## 波及

- **追加の作業は無い**: 回答時点で PR #916（2026-08-15T14:49Z）と PR #917（2026-08-15T15:01Z）はどちらも merged 済み。A の想定どおり月シートが消え、#917 の見送りが結果的に正解になったため、月シート全画面化の Issue 起票は不要
