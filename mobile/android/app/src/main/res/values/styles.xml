<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!--
          Safe-area fallback (Phase 4). targetSdk 36 enforces edge-to-edge, so
          the WebView draws under the status/navigation bars. fitsSystemWindows
          pads the content view by the system-bar insets so UI is not clipped
          under the notch / gesture bar. The richer fix (viewport-fit=cover +
          CSS env(safe-area-inset-*) in web/index.html) is a stream-E-coordinated
          follow-up — see the Phase 4 plan handoff. This native fallback keeps
          the wrapper inside mobile/android/ and works without touching web/.
        -->
        <item name="android:fitsSystemWindows">true</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>